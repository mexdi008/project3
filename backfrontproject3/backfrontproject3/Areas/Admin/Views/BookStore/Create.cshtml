<div class="container">
    <div class="row">
        <div class="col-md-6 grid-margin stretch-card">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Slider - Edit</h4>
                    <form method="post" enctype="multipart/form-data" class="forms-sample">
                        <div class="form-group">
                            @*<label asp-for=@Model.Title>Title</label>
                            <input asp-for=@Model.Title type="text" class="form-control" placeholder="Title">
                            <span asp-validation-for="@Model.Title" class="text-danger mt-3"></span>
                        </div>
                        <div class="form-group">
                            <label asp-for=@Model.Body>Body</label>
                            <input asp-for=@Model.Body type="text" class="form-control" placeholder="Body">
                            <span asp-validation-for="@Model.Body" class="text-danger mt-3"></span>
                        </div>
                        <div class="form-group">
                            <label asp-for=@Model.MainImage> Main Image</label>
                            <input accept="image/*" asp-for=@Model.MainImage class="form-control">
                            <span asp-validation-for="@Model.MainImage" class="text-danger mt-3"></span>
                        </div>
                        <div class="form-group">
                            <label asp-for=@Model.BackGroundImage> Background Image</label>
                            <input accept="image/*" asp-for=@Model.BackGroundImage class="form-control">
                            <span asp-validation-for="@Model.BackGroundImage" class="text-danger mt-3"></span>*@
                        </div>
                        <button type="submit" class="btn btn-primary mr-2">Submit</button>
                        <a asp-area="admin" asp-controller="parallax" asp-action="index" class="btn btn-light">Cancel</a>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>